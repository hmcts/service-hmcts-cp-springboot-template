apiVersion: v1
kind: Service
metadata: { name: case-document-knowledge-service, labels: { app: case-document-knowledge-service } }
spec:
  type: ClusterIP
  selector: { app: case-document-knowledge-service }
  ports: [ { name: http, port: 80, targetPort: 8080 } ]
